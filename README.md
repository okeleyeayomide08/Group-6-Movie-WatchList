### Installed Express.

### Installed dotenv.

### Installed UUID for unique Id generation.

### Installed Nodemon as Dev Dependencies.

### We are using ESM (ECMAScript Modules) - already configured.

### The port to the Server is port 5000.

### To start the server run npm run start or npm run dev to run nodemon.
